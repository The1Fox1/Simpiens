You are crossing the threshold from a basic state machine into the territory of a deeply systemic narrative engine. To get agents to coordinate attacks, hold grudges, and execute long-term goals without melting your CPU, you have to fundamentally change how they process time and context.

Utility AI is great for immediate biological drives (Hunger, Sleep), but "build a tribe and wage war" requires multi-step planning and retained knowledge.

Here are the architectural epics required to build this out, ordered by structural dependency, along with estimated phases for fleshing them out.

Epic 5: The Memory & Knowledge Matrix [COMPLETED]
An agent cannot plot revenge or form a tribe if it operates purely in the present tense. Agents now remember the past and store it efficiently with decaying spatial maps, event ledgers, and peer gossip.

Feature 1: Local Knowledge Grids (Completed). Agents have personal Fog of War memory maps, temporal decay (Pawns: 400 ticks, Resources: 1500 ticks), and line-of-sight pruning.

Feature 2: Event Ledgers & Gossip (Completed). Agents maintain ring-buffer event ledgers, track social interaction cooldowns, exchange spatial/event memories during IdleIntents when within 2.5 units, and relieve frustration.

Epic 6: Multi-Step Reasoning (GOAP / HTN) [COMPLETED]
Basic utility AI only solves one step at a time. To craft a weapon, an agent must first find wood, then find stone, then find a workbench.

Feature 1: Goal-Oriented Action Planning (GOAP).
- Phase 1 (Completed): Zero-allocation bitmask WorldState, GoapAction definitions with preconditions & effects, GoapGoal structures, and ActionGraph prerequisite dependency resolution.
- Phase 2 (Completed): High-performance A* search through the action graph (GoapPlanner) with pre-allocated node pools, binary min-heap, open-addressing closed table, branch cost optimization, and 0-allocation plan reconstruction.
- Phase 3 (Completed): Integration of the GOAP planner into the background UniTask pipeline with reflexive utility AI interruption, in-flight intent preemption (ISimulationManager.AbortIntent), WorldStateBuilder, and sequential plan step execution.

Epic Alpha: Biological Needs, Mental Breaks & Autonomous Motor Watchdog [COMPLETED]
Before agents can form tribes or wage wars (Epic 7), their individual autonomy must be resilient and biologically grounded. This epic decouples physical navigational stalls from internal biological drives, introduces a multifaceted needs hierarchy, and establishes critical mental breaks (panic/starvation) that override deliberate planning.

Feature 1: Multifaceted Biological Drives & GOAP Goal Dynamic Priority.
- Phase 1 (Completed): Structured AgentNeeds (Hunger, Energy, Social) with continuous decay, WorldState bitmask extensions, dynamic GOAP goal priorities (SatiateHungerGoal, RestGoal, SocializeGoal), and clean drive replenishment.

Feature 2: Autonomous Motor Watchdog (Unstuck Failsafe).
- Phase 2 (Completed): Navigation watchdog in SimulationManager/NodeController detecting physical deadlocks, zero positional progress, and repeated path obstruction; implements progressive multi-tiered unstuck reflexes (local clearance nudge, relaxed repath, target disengage) without corrupting biological or emotional state.

Feature 3: Mental Breaks & Emergency Reflexes (Starvation Panic & Exhaustion Collapse).
- Phase 3 (Completed): Critical drive thresholds in Tier 1 Reflexive Evaluators (MentalBreakEvaluator). Starvation Panic (Hunger >= 85f and no known food) wipes stale resource memory, triggers red panic pose, and launches 1.5x exploratory sprint; Exhaustion Collapse (Energy <= 5f) forces emergency rest until Energy >= 30f.

Feature 4: Visual Feedback & Domain Validation Suite.
- Phase 4 (Completed): Integration of distinct visual states (Harvesting, Panicking, Resting, Gossiping, Walking, Thinking, Idle) with procedural micro-animations in AgentView, and master domain validation suite DriveSystemValidator in Assets/Scripts/Testing/Drives/ verifying 0 GC allocations, watchdog recovery, and panic/collapse transitions.

Current Focus: Epic 7: The Sociopolitical & Relationship System
With memory and planning in place, agents can now factor other agents into their goals.

Feature 1: The Social Affinity Matrix & Relational Dynamics.
- Phase 1 (Completed): Sparse, zero-allocation relationship records in AgentMemory tracking Warmth (-100 to +100), Trust (-100 to +100), Fear (0 to 100), and Respect (-100 to +100). Interpersonal gossip increases Warmth/Trust/Respect and relieves Fear, while distrust/fear suppresses gossip. GOAP WorldState integration with relational facts (HasWarmCompanion, HasTrustedPeer, HasFearedThreat, HasRespectedLeader).

Feature 2: Tribal Tagging & Authority Hierarchy.
- Phase 2 (Active): Agents maintain TribeId and TribalRole (Chieftain, Elder, Follower, Outcast). Chieftain commands propagate via the InfluenceQueue / PlayerInterventionService as cognitive signals rather than direct state mutations. Inter-tribal diplomacy seeds baseline affinities.

Epic 8: Inventory, Economy & Construction
Agents need to manipulate the world permanently to build civilizations. Physical resource pooling belongs here once inventory and storage structures exist.

Feature 1: Entity Inventories & Item Transport.
- Phase 1: Stack-allocated personal inventories, item pick-up, drop-off, and container transfer from global ResourceData.

Feature 2: Tribal Resource Pooling & World Construction.
- Phase 2: Shared tribe stockpiles and the Build Intent pipeline, allowing agents to place structures (e.g. walls) directly into the SpatialHashGrid.

Epic 9: Martial Engagement & Tactical Reasoning
Combat is the most intensive system because it requires rapid, highly volatile state mutations.

Feature 1: Health, Damage, and Projectile Data. Adding combat stats to the AgentContext and defining data structures for transient entities like flying arrows.

Feature 2: Tactical Grid Evaluation. During combat, the cognitive engine must evaluate tiles not just for pathfinding, but for safety (cover, distance from threat, line of sight).

Estimated Effort: 3 Phases. Phase 1 builds the melee combat state machine and damage resolution on the main thread. Phase 2 implements ranged combat and line-of-sight math. Phase 3 introduces tactical pathfinding (fleeing, flanking) in the background threads.

Epic 10: The Agentic Narrative Engine (MCP Integration)
This is the ultimate layer. Instead of hardcoding why an agent wants to start a war, you hand their Memory Ledger and Sociopolitical Matrix to an external model context protocol.

Feature 1: Payload Serialization. Converting the complex C# AgentContext and Memory Ledger into a lightweight JSON payload.

Feature 2: The LLM Evaluator. An ICognitiveEvaluator that fires off an asynchronous request to your model, receives the narrative reasoning, and translates it back into a concrete AgentIntent (e.g., AttackTribe(TribeId)).

Estimated Effort: 2 Phases. Phase 1 builds the serialization and the asynchronous network call layer. Phase 2 handles prompt engineering, response parsing, and error handling for hallucinations or invalid commands.